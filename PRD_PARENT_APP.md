# Reading Buddy — Parent App PRD

> **Version:** 1.0 | **작성일:** 2026-03-17
> **문서 성격:** 부모님 전용 앱 제품 요구사항 (메인 PRD와 별개)
> **연관 문서:** `PRD.md` (AI 스피커 + 콘텐츠 시스템)

---

## 목차

1. [제품 철학 및 포지셔닝](#1-제품-철학-및-포지셔닝)
2. [앱 개요 및 핵심 기능 구조](#2-앱-개요-및-핵심-기능-구조)
3. [기능 상세: 스피커 원격 제어](#3-기능-상세-스피커-원격-제어)
4. [기능 상세: 이용 시간 및 스케줄 관리](#4-기능-상세-이용-시간-및-스케줄-관리)
5. [기능 상세: 카드 및 콘텐츠 관리](#5-기능-상세-카드-및-콘텐츠-관리)
6. [기능 상세: 리포트 및 학습 현황](#6-기능-상세-리포트-및-학습-현황)
7. [핵심 차별점: 부모님 대화 가이드](#7-핵심-차별점-부모님-대화-가이드)
8. [기능 상세: 하드웨어 연계 (선택적 스크린 브릿지)](#8-기능-상세-하드웨어-연계-선택적-스크린-브릿지)
9. [기술 아키텍처](#9-기술-아키텍처)
10. [개발 로드맵](#10-개발-로드맵)
11. [향후 발전 방향](#11-향후-발전-방향)

---

## 1. 제품 철학 및 포지셔닝

### 1-1. 핵심 철학: 기계가 아닌 가족을 연결하는 도구

Reading Buddy의 스피커는 **아이와 AI의 1:1 음성 대화** 경험을 제공합니다.
하지만 이 서비스가 궁극적으로 추구하는 것은 AI와의 대화가 아닙니다.

> **"오늘 스피커에서 배운 것을, 저녁 식탁에서 가족과 나누는 것."**
>
> AI는 아이와 가족 사이의 대화를 *대체*하는 것이 아니라 *촉매*가 되어야 합니다.
> 부모님 앱은 그 연결 고리입니다.

### 1-2. 스크린 프리 원칙 (Screen-Free First)

| 대상 | 원칙 | 이유 |
|---|---|---|
| 아이 (4~7세) | **스크린 없음** | 청각·언어·상상력 중심 발달 지원. 화면 의존 없이 대화로만 학습 |
| 부모님 | **스마트폰/태블릿 앱 사용** | 콘텐츠 설정, 현황 파악, 대화 가이드 수신 용도 |
| 함께하는 시간 | **오프라인 대화** | 앱이 제안한 대화 주제로 화면 없이 가족이 직접 이야기 |

### 1-3. 부모님 앱의 역할 정의

```
[AI 스피커] ──── 아이의 학습·놀이 경험 ────▶ [부모님 앱]
                                                    │
                                          ┌─────────┴─────────┐
                                          │                   │
                               스피커 제어·설정       오늘의 대화 가이드
                               (관리 도구)            (연결 도구 ★)
```

부모님 앱은 두 가지 역할을 동시에 수행합니다:
- **관리 도구**: 스피커를 원격으로 제어하고, 콘텐츠와 이용 시간을 설정
- **연결 도구**: 아이의 오늘 활동을 바탕으로, 가족이 직접 나눌 대화를 제안

---

## 2. 앱 개요 및 핵심 기능 구조

### 2-1. 앱 구성 화면 (Navigation)

> **MVP 구현 방식:** Next.js `/parent` 라우트 그룹으로 구현.
> 데스크톱 브라우저에서는 스마트폰 목업 프레임 안에 렌더링되어 앱처럼 보이고,
> 실제 모바일 기기로 접속 시 전체 화면 모바일 서비스로 동작합니다.
> admin(`/admin`)과 라우트·코드·UI 모두 완전히 분리됩니다.

```
┌──────────────────────────────────────────────────────┐
│  [데스크톱 브라우저에서 본 모습]                       │
│                                                      │
│        ┌────────────────────┐                        │
│        │  📱 스마트폰 목업   │                        │
│        │  ┌──────────────┐  │                        │
│        │  │  앱 UI 렌더링 │  │                        │
│        │  └──────────────┘  │                        │
│        │  [홈][리포트][가이드][설정] │                 │
│        └────────────────────┘                        │
└──────────────────────────────────────────────────────┘

라우트 구조:
  /parent/home         홈 화면
  /parent/reports      리포트
  /parent/talk-guide   대화 가이드
  /parent/settings     설정
```

```
홈 (/parent/home)
├── 오늘의 대화 가이드 카드 (★ 최우선 노출)
├── 현재 스피커 상태 (재생 중 / 대기 / 오프라인)
├── 빠른 제어 (재생/정지/다음 카드)
└── 오늘 이용 시간 요약

리포트 (/parent/reports)
├── 이용 현황 (일/주/월)
├── 학습 현황 (카드별, 단어별)
└── 성장 리포트 (월간 발달 요약)

대화 가이드 (/parent/talk-guide) ★ 핵심 차별점
├── 오늘의 대화 주제
├── 추천 질문 카드
├── 오늘 배운 단어 설명
└── 같이 해볼 활동 제안

설정 (/parent/settings)
├── 스피커 기기 설정
├── 아이 프로필 관리
├── 카드 및 콘텐츠 관리
├── 이용 시간 설정
└── 알림 설정
```

### 2-2. 주요 기능 목록

| 기능 | 우선순위 | 카테고리 |
|---|---|---|
| 부모님 대화 가이드 | **P0** | 연결 도구 |
| 스피커 원격 제어 (재생/정지/전환) | P0 | 관리 도구 |
| 이용 현황 리포트 | P0 | 리포트 |
| 이용 시간 제한 설정 | P0 | 관리 도구 |
| 카드별 콘텐츠 설정 | P0 | 관리 도구 |
| 학습 현황 리포트 | P1 | 리포트 |
| 아이 프로필 다중 관리 | P1 | 관리 도구 |
| 스케줄 설정 (요일/시간대) | P1 | 관리 도구 |
| 월간 성장 리포트 | P1 | 리포트 |
| 카메라 연계 (스토리 창작) | P2 | 스크린 브릿지 |
| 학교/원 연계 | P3 | 연결 확장 |

---

## 3. 기능 상세: 스피커 원격 제어

### 3-1. 개요

NFC 카드 없이도 부모님이 스피커의 콘텐츠를 직접 제어합니다.
아이가 잠든 후 내일 들을 콘텐츠를 미리 설정하거나, 아이가 스피커에서 멀리 있을 때 콘텐츠를 바꿔줄 수 있습니다.

### 3-2. 제어 기능

```
현재 재생 상태 표시
├── 재생 중인 카드명 + 카드 커버 이미지
├── 현재 세션 상태 (PRE / DURING / POST / IDLE)
├── 현재 페이지 (Read with me 카드: "3 / 7")
└── 오늘 총 이용 시간

빠른 제어 버튼
├── [▶ / ⏸] 재생 / 일시정지
├── [⏭] 다음 카드로 전환
├── [⏹] 세션 종료
└── [🔊] 볼륨 조절

카드 직접 실행
├── 카드 목록에서 선택 → 스피커에서 즉시 시작
├── 특정 카드를 "오늘의 카드"로 예약 설정
└── NFC 태그 없이 앱에서 직접 콘텐츠 전환
```

### 3-3. 연결 방식

```
부모님 앱  ──WebSocket/HTTP──▶  Reading Buddy 서버  ──▶  AI 스피커 하드웨어
           (원격 명령 전송)      (명령 처리 + 상태 저장)   (명령 실행)
```

- 스피커와 앱은 동일 가정 Wi-Fi 또는 인터넷을 통해 연결
- 스피커 상태는 실시간 동기화 (WebSocket 구독)
- 현재 프로토타입에서 `/api/admin/cards` 구조를 기반으로 확장

---

## 4. 기능 상세: 이용 시간 및 스케줄 관리

### 4-1. 일일 이용 시간 제한

```
이용 시간 설정
├── 하루 최대 이용 시간: [30분 / 1시간 / 1.5시간 / 제한없음]
├── 남은 시간 표시 (부모 앱 + 스피커 음성 알림)
│   └── "오늘은 30분 더 사용할 수 있어!"
├── 종료 5분 전 음성 알림: "조금 있으면 오늘 마무리할 시간이야~"
└── 초과 시 자동 종료 + 부모 앱 알림
```

### 4-2. 스케줄 설정

```
요일별 / 시간대별 이용 가능 설정

예시:
  평일 오후 4시 ~ 6시: 이용 가능
  평일 저녁 9시 이후: 이용 불가
  주말 오전 8시 ~ 11시: 이용 가능

시각적 설정 UI: 주간 타임라인 그리드
  (각 시간대를 탭해서 활성/비활성 토글)
```

### 4-3. 특별 모드

```
집중 학습 모드 (부모 승인 후 연장)
├── 평소 제한 시간 초과 시 → 부모 앱 승인 요청 알림
└── 부모가 앱에서 [+30분 허용] 탭 → 스피커에서 계속 진행

방해 금지 모드 (취침 시간 등)
├── 설정한 시간대에는 카드 태그해도 작동 안 함
└── 스피커 음성: "지금은 쉬는 시간이야. 내일 또 놀자!"
```

---

## 5. 기능 상세: 카드 및 콘텐츠 관리

### 5-1. 아이 프로필 관리

실제 제품에서는 한 스피커를 여러 아이가 공유할 수 있습니다.
NFC 카드에 아이 정보를 함께 담아 자동 전환하거나, 앱에서 현재 사용 아이를 선택합니다.

```
프로필 생성/편집
├── 이름, 생년월일, 아바타 선택
├── 관심사 태그 (우주, 공룡, 동물, 운동, 음악 등)
├── 언어 설정 (한국어 / 영어 / 이중언어)
└── 개별 이용 시간 제한 (아이마다 다르게 설정)

최대 5명 프로필 지원 (가족 구성)
```

### 5-2. 카드별 허용/차단

```
카드 활성화 제어
├── 카드별 ON/OFF 토글 (특정 카드 숨기기)
├── 연령별 필터 (만 4세 / 만 5~6세 / 만 7세+)
└── 콘텐츠 난이도 조정 (쉬움 / 보통 / 어려움)
    └── AI 프롬프트에 난이도 파라미터로 주입

Read with me 카드 관리
├── PDF 도서 업로드 (현재 구현된 book-data-analyzer 연동)
├── 독서 속도 설정 (천천히 / 보통 / 빠르게)
└── 읽기 모드 선택 (대화형 / 듣기만 / 따라읽기)
```

### 5-3. 카드 추천 설정

```
추천 카드 설정
├── 오늘의 추천 카드 수동 지정
├── 자동 추천 ON/OFF
│   └── 최근 학습 이력 기반 AI 추천
│       예: "수학을 3일 연속 안 했어요. Math Battle 추천!"
└── 새 카드 알림 (신규 콘텐츠 업데이트 시 푸시 알림)
```

---

## 6. 기능 상세: 리포트 및 학습 현황

### 6-1. 이용 현황 리포트

```
일간 현황
├── 오늘 이용 시간 (카드별 분류)
├── 이용 카드 목록 + 각 세션 시간
├── 총 대화 횟수
└── 세션 완료 여부 (Read with me PRE/DURING/POST 완주 여부)

주간 현황
├── 요일별 이용 시간 막대그래프
├── 가장 많이 이용한 카드 TOP 3
└── 주간 연속 이용일 (스트릭)

월간 현황
├── 총 학습 시간 누적
├── 완료한 Read with me 도서 목록
└── 이번 달 새로 배운 단어 수
```

### 6-2. 학습 현황 리포트

현재 구현된 `BookData.pages[].vocabulary`와 AI 대화 내 단어 추출을 기반으로 합니다.

```
단어/어휘 현황
├── 이번 주 새로 배운 단어 목록
│   예: "블랙홀, 중력, 성운, 초성..."
├── 단어 카테고리 분포 (과학 / 언어 / 수학 / 일상)
└── 단어 복습 필요 알림
    └── "3일 동안 복습 안 한 단어가 12개 있어요"

게임 현황 (카드별)
├── Mindreader: 평균 질문 수 / 정답률
├── Math Battle: 정답률 / 최고 연속 정답 수
├── Riddles: 힌트 없이 맞힌 비율
├── Market Game: 평균 기억 개수
└── Reverse Word: 평균 반응 속도

Read with me 현황
├── 완독한 책 목록
├── 독서 단계 완료율 (PRE/DURING/POST)
├── 읽은 페이지 수 누적
└── 독후 질문 참여도
```

### 6-3. 성장 리포트 (월간, AI 생성)

```
월간 AI 성장 분석 리포트
├── 이달의 학습 하이라이트
│   예: "이번 달 우주 관련 단어를 23개 배웠어요 🚀"
├── 두드러진 성장 영역
│   예: "수학 정답률이 지난달 대비 15% 향상됐어요"
├── 아이의 관심사 분석
│   예: "공룡, 바다 생물, 수학 순으로 관심이 높아요"
├── 권장 다음 단계
│   예: "어휘력이 충분히 쌓였어요. 영어 카드 도전을 권장해요"
└── 성장 포인트 요약 (부모 공유용 카드 이미지 생성)
```

---

## 7. 핵심 차별점: 부모님 대화 가이드

> ★ **이 기능이 Reading Buddy를 단순한 학습 도구가 아닌 가족 연결 플랫폼으로 만드는 핵심입니다.**

### 7-1. 철학

아이가 AI 스피커와 30분 동안 우주에 대해 배웠습니다.
그 30분이 끝난 후, 무엇이 남아야 할까요?

단순한 정보가 아니라 **"엄마, 블랙홀이 뭔지 알아?"** 라는 질문이 남아야 합니다.
부모님 대화 가이드는 아이가 오늘 경험한 것을 바탕으로,
**저녁 식탁에서 가족이 자연스럽게 나눌 수 있는 대화의 씨앗**을 부모님 손에 쥐어줍니다.

### 7-2. 기능 동작 방식

```
[스피커 세션 종료]
       │
       ▼
[세션 데이터 수집]
 ├── 오늘 이용한 카드: Astro The Explorer
 ├── 대화 내용 요약: 블랙홀, 태양계, 중력에 대해 배움
 ├── 아이 반응: "블랙홀" 단어에 특히 많이 반응
 ├── 질문한 내용: "블랙홀에 빠지면 어떻게 돼요?"
 └── 게임 결과: 수수께끼 3개 중 2개 정답
       │
       ▼
[AI 대화 가이드 생성] (GPT-4o)
 ├── 오늘의 대화 주제 카드 생성
 ├── 부모님용 배경 지식 요약 (1-2문장)
 ├── 아이에게 물어볼 추천 질문 3가지
 ├── 아이가 대답했을 때 이어갈 대화 팁
 ├── 같이 해볼 수 있는 오프라인 활동 제안
 └── 책/영상 연계 추천 (선택)
       │
       ▼
[부모님 앱 푸시 알림]
"오늘 ○○이가 블랙홀에 대해 궁금해했어요. 대화 가이드를 확인해보세요 💬"
```

### 7-3. 대화 가이드 카드 UI

```
┌─────────────────────────────────────┐
│  🚀 오늘의 대화 주제                 │
│  "블랙홀과 우주의 신비"              │
├─────────────────────────────────────┤
│  💡 오늘 ○○이가 배운 것             │
│  블랙홀이 무엇인지, 중력이 얼마나    │
│  강한지, 태양계에 대해 이야기했어요. │
│  특히 "블랙홀에 빠지면 어떻게 되나요?" │
│  라는 질문을 했어요.                 │
├─────────────────────────────────────┤
│  🗣️ 이렇게 물어보세요               │
│                                     │
│  ① "오늘 Astro한테 뭐 배웠어?"      │
│  ② "블랙홀이 얼마나 큰 것 같아?"    │
│  ③ "우주에 가면 뭘 제일 보고 싶어?" │
├─────────────────────────────────────┤
│  🎮 같이 해볼 활동                   │
│  밤에 창문으로 별 찾아보기           │
│  종이에 태양계 행성 그려보기         │
├─────────────────────────────────────┤
│  📚 연계 도서 추천                   │
│  "우주 대백과" (국립어린이도서관)    │
└─────────────────────────────────────┘

              [대화 시작했어요 ✓]
```

### 7-4. 대화 가이드 생성 API 설계

```typescript
// POST /api/parent/talk-guide
Request: {
  profile_id: string,
  session_id: string,
  session_summary: {
    card_id: string,
    card_type: string,
    duration_seconds: number,
    topics_covered: string[],       // AI가 세션 중 다룬 주제
    child_questions: string[],      // 아이가 한 질문들
    highlighted_words: string[],    // 아이가 반응한 단어
    game_results?: GameResult,      // 게임 카드의 경우
    book_pages_completed?: number   // Read with me의 경우
  }
}

Response: {
  talk_guide: {
    title: string,                  // "오늘의 대화 주제"
    summary_for_parent: string,     // 부모님용 오늘 활동 요약 (2-3문장)
    child_highlight: string,        // 아이가 특별히 반응한 포인트
    conversation_starters: {        // 추천 대화 시작 질문 3개
      question: string,
      why_this_works: string        // 이 질문이 효과적인 이유 (부모 참고용)
    }[],
    offline_activities: string[],   // 함께 해볼 오프라인 활동 2-3개
    book_recommendations?: {        // 연계 도서 (선택)
      title: string,
      reason: string
    }[]
  }
}
```

### 7-5. 대화 가이드 개인화 수준 (발전 단계)

**v1 (기본):** 오늘 이용한 카드 기반 단순 가이드
```
Astro 카드 이용 → 우주 관련 대화 주제 + 질문 3개
```

**v2 (개인화):** 아이의 반응 패턴 분석 반영
```
"○○이는 '왜' 질문을 많이 해요. 원인을 묻는 질문이 효과적이에요."
"숫자와 크기 비교를 좋아해요. 크기 관련 질문을 시도해보세요."
```

**v3 (성장 연계):** 발달 단계 + 장기 관심사 추적
```
"지난 3개월간 우주, 바다, 공룡에 흥미를 보였어요.
이 아이는 자연과학 탐구형이에요. 과학 실험 책 읽기를 추천해요."
```

**v4 (가족 확장):** 형제, 조부모, 다른 가족 참여 유도
```
"할아버지께 ○○이가 오늘 배운 블랙홀 이야기를 해보라고 해보세요.
세대 간 대화로 이어질 수 있어요."
```

---

## 8. 기능 상세: 하드웨어 연계 (선택적 스크린 브릿지)

> **원칙:** 스크린 프리가 메인 컨셉입니다.
> 아래 기능은 스마트폰/태블릿 카메라·화면이 **경험을 극적으로 향상**시키는 경우에만 선택적으로 활용합니다.
> "있으면 좋다"는 이유만으로 화면을 도입하지 않습니다.

### 8-1. 카메라 연계: 스토리 창작 강화

**활성화 조건:** Story Writer 카드 사용 중 + 부모님이 앱에서 카메라 연계 허용 시

```
사용 흐름:
1. 아이가 스피커와 스토리를 완성한다 (음성으로만)
2. 스피커: "그림으로도 그려볼까? 엄마/아빠 핸드폰을 열어봐!"
3. 부모님 앱: 그림 그리기 or 카메라로 아이 그림 촬영 모드 활성화
4. 아이가 직접 그린 그림을 촬영
5. GPT-4o Vision으로 그림 분석
6. 스피커: "와, 정말 용이 하늘을 날고 있구나! 이름이 뭐야?"
7. 분석된 그림 + 스토리 JSON을 함께 저장
```

**이 기능이 정당화되는 이유:**
- 아이의 창의적 산출물(그림)을 AI 대화에 반영하여 경험이 질적으로 향상됨
- 화면을 직접 보는 것이 아니라, 카메라를 매개로 오프라인 활동(그림 그리기)을 연결
- 부모가 함께 참여하는 시간이 됨

### 8-2. 화면 연계: Read with me 그림책 보기

**활성화 조건:** 부모님이 명시적으로 활성화 + 아이 연령 6세 이상 설정 시만

```
사용 흐름:
1. 평소에는 스피커 음성만으로 Read with me 진행 (스크린 프리)
2. 부모님이 "오늘은 그림도 같이 볼까?" → 앱에서 책 화면 표시 모드 ON
3. 부모님 앱(태블릿)에 책 삽화가 표시됨
4. 아이와 부모가 태블릿을 함께 보며, 스피커 음성을 같이 듣는 형태

핵심: 아이 혼자 화면을 보는 것이 아니라,
      부모와 아이가 함께 보는 "공동 독서" 형태로만 활성화
```

**제한 조건:**
- 부모 동반 없이 아이 혼자는 화면 활성화 불가
- 기본값은 항상 스크린 프리 (음성 전용)
- 부모가 앱을 닫으면 자동으로 음성 전용 모드로 복귀

### 8-3. 비고: 도입하지 않는 하드웨어 기능

| 기능 | 제외 이유 |
|---|---|
| 스피커에 화면 내장 | 스크린 프리 핵심 컨셉 위배 |
| 아이 혼자 태블릿으로 콘텐츠 이용 | 화면 의존성 생성 위험 |
| 얼굴 인식 카메라 | 아동 프라이버시 민감도, 불필요한 복잡성 |
| 실시간 영상 통화 연계 | 핵심 가치와 무관한 기능 팽창 |

---

## 9. 기술 아키텍처

### 9-1. 앱 플랫폼

**MVP (프로토타입):** Next.js 웹앱 내 `/parent` 라우트
```
현재 프로젝트(Next.js 14)에 부모님 앱 전용 라우트를 추가
├── /parent                  모바일 목업 셸 (스마트폰 프레임 렌더링)
│   ├── /parent/home         홈 화면
│   ├── /parent/reports      리포트
│   ├── /parent/talk-guide   대화 가이드
│   └── /parent/settings     설정
└── admin과 완전히 분리된 독립 라우트 (공유 없음)

구현 방식:
- 브라우저에서 열면 스마트폰 목업 프레임 안에 앱 UI가 표시됨
- 실제 모바일 기기에서 열면 그 자체로 모바일 서비스처럼 동작
- admin 화면과 코드/라우트/UI 완전 분리
- 모바일 뷰포트 기준 (max-width: 390px) 로 설계
```

**실제 앱 (Phase 2 이후):** React Native (Expo) 또는 Flutter
```
├── iOS + Android 동시 지원
├── 푸시 알림 (FCM / APNs)
├── 로컬 알림 (이용 시간 경고)
└── 카메라 API 접근
```

> MVP에서 검증된 화면 설계와 API를 그대로 네이티브 앱으로 이식하는 구조입니다.

### 9-2. 백엔드 연동 구조

```
현재 프로토타입 서버 (Next.js API Routes)
         │
         │  기존 API 재활용 + 신규 API 추가
         ▼
┌─────────────────────────────────────────┐
│  Reading Buddy API Server               │
│                                         │
│  기존 (프로토타입에서 이미 구현)         │
│  ├── /api/admin/cards         카드 CRUD │
│  ├── /api/chat                AI 대화   │
│  ├── /api/realtime            음성 토큰 │
│  └── /api/story/finalize      스토리    │
│                                         │
│  신규 (부모님 앱용)                      │
│  ├── /api/parent/profiles     프로필    │
│  ├── /api/parent/sessions     세션 로그 │
│  ├── /api/parent/reports      리포트    │
│  ├── /api/parent/talk-guide   대화가이드│
│  ├── /api/parent/schedule     스케줄    │
│  └── /api/speaker/control    원격 제어  │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  AI 스피커 하드웨어                      │
│  ├── 실시간 상태 WebSocket 구독          │
│  └── 원격 제어 명령 수신                 │
└─────────────────────────────────────────┘
```

### 9-3. 데이터 모델 (신규 추가)

```typescript
// 아이 프로필
interface ChildProfile {
  profile_id: string;
  name: string;
  birth_date: string;
  avatar: string;
  interests: string[];
  language: "ko" | "en" | "both";
  daily_limit_minutes: number;
  allowed_card_ids: string[];       // 빈 배열 = 전체 허용
  difficulty_level: "easy" | "normal" | "hard";
}

// 세션 로그 (현재 세션 종료 시 저장)
interface SessionLog {
  session_id: string;
  profile_id: string;
  card_id: string;
  card_type: string;
  started_at: string;
  ended_at: string;
  duration_seconds: number;
  topics_covered: string[];
  child_questions: string[];
  highlighted_words: string[];
  pages_completed?: number;
  game_results?: {
    total_rounds: number;
    correct_answers: number;
    hints_used: number;
  };
  talk_guide_generated: boolean;
}

// 이용 스케줄
interface UsageSchedule {
  profile_id: string;
  weekly_schedule: {
    [day: string]: {       // "mon" | "tue" | ... | "sun"
      allowed: boolean;
      time_ranges: { start: string; end: string }[];
    }
  };
  do_not_disturb_after: string;  // "21:00"
}

// 대화 가이드
interface TalkGuide {
  guide_id: string;
  profile_id: string;
  session_id: string;
  generated_at: string;
  title: string;
  summary_for_parent: string;
  child_highlight: string;
  conversation_starters: {
    question: string;
    why_this_works: string;
  }[];
  offline_activities: string[];
  book_recommendations: { title: string; reason: string }[];
  parent_acknowledged: boolean;    // 부모가 확인했는지
  conversation_happened: boolean;  // 부모가 "대화 시작했어요" 탭했는지
}
```

### 9-4. 저장소 전략

| 단계 | 저장소 | 이유 |
|---|---|---|
| MVP (웹 목업) | JSON 파일 (`data/parent/`) | 현재 시스템과 일관성 유지, 별도 DB 세팅 불필요 |
| 네이티브 앱 출시 | Supabase (PostgreSQL) | 다중 기기 동기화, 인증 필요 |
| 성장 단계 | Supabase + Redis 캐시 | 실시간 리포트, 빠른 조회 |

---

## 10. 개발 로드맵

### Phase 1: 웹 목업 MVP (현재 프로젝트 내 `/parent` 라우트)

> **목표:** admin과 완전히 분리된 독립 라우트로 부모님 앱을 구현.
> 브라우저에서는 스마트폰 목업 프레임 안에 앱처럼 표시되고,
> 실제 모바일 기기로 접속하면 그 자체로 모바일 서비스처럼 동작한다.

```
라우트 구조 (신규 추가):
  /parent                  ← 모바일 목업 셸 진입점
  /parent/home             ← 홈 (스피커 상태 + 오늘의 대화 가이드)
  /parent/reports          ← 이용/학습 현황 리포트
  /parent/talk-guide       ← 대화 가이드 목록 + 상세
  /parent/settings         ← 설정 (프로필, 이용시간, 카드관리)

구현 항목:
  ✅ 이미 구현: 카드 CRUD API, 프롬프트 버전 관리
  🔲 /parent 라우트 그룹 및 모바일 목업 레이아웃 컴포넌트
      - 데스크톱: 스마트폰 프레임(목업) 안에 앱 UI 렌더링
      - 모바일 기기 직접 접속: 전체 화면으로 앱처럼 동작
  🔲 하단 탭 네비게이션 (홈 / 리포트 / 대화가이드 / 설정)
  🔲 세션 로그 저장 API + JSON 기록 (data/parent/sessions/)
  🔲 홈 화면: 스피커 현재 상태 + 오늘의 대화 가이드 카드
  🔲 대화 가이드 생성 API (/api/parent/talk-guide)
  🔲 대화 가이드 상세 화면
  🔲 이용 현황 리포트 화면 (일/주 그래프)
  🔲 아이 프로필 설정 (최대 3명)
  🔲 일일 이용 시간 제한 설정

검증 목표:
  - 대화 가이드 AI 품질 검증
  - 모바일 UX 흐름 사전 검증 (네이티브 앱 전환 전)
  - 부모님 반응 및 핵심 기능 우선순위 파악
```

### Phase 2: 네이티브 모바일 앱

> Phase 1 목업에서 검증된 화면/기능을 네이티브 앱으로 이식.
> `/parent` 라우트의 API와 데이터 구조를 그대로 재활용.

```
구현 항목:
  🔲 React Native (Expo) 앱 프로젝트 세팅
  🔲 부모 계정 인증 (이메일/소셜 로그인, Supabase Auth)
  🔲 Phase 1 화면 전체 네이티브 컴포넌트로 이식
  🔲 스피커 원격 제어 (WebSocket 실시간 연동)
  🔲 스케줄 설정 (요일/시간대)
  🔲 푸시 알림 (대화 가이드 도착, 이용 시간 경고)
  🔲 DB 전환: JSON 파일 → Supabase

예상 소요: 6-8주
```

### Phase 3: 학습 현황 + 대화 가이드 고도화

```
구현 항목:
  🔲 단어장 자동 생성 + 단어 리포트
  🔲 게임별 학습 현황 분석
  🔲 월간 AI 성장 리포트
  🔲 대화 가이드 v2 (아이 반응 패턴 반영)
  🔲 오프라인 활동 데이터베이스 구축
  🔲 도서 연계 추천 (국립어린이청소년도서관 API)

예상 소요: 8-10주
```

### Phase 4: 하드웨어 연계 + 커뮤니티

```
구현 항목:
  🔲 카메라 연계 (스토리 그림 촬영 → GPT-4o Vision)
  🔲 공동 독서 모드 (부모 동반 화면 활성화)
  🔲 부모 커뮤니티 (대화 가이드 공유, 육아 팁)
  🔲 대화 가이드 v3 (발달 단계 연계)

예상 소요: 10-12주
```

---

## 11. 향후 발전 방향

### 11-1. 가족 대화 데이터 축적

부모님이 "대화 시작했어요 ✓" 버튼을 탭하면, 어떤 주제가 실제 가족 대화로 이어졌는지 데이터가 쌓입니다.

```
축적 데이터 활용:
- 어떤 카드/주제가 가족 대화 전환율이 높은가
- 어떤 질문 형식이 더 잘 통하는가
- 아이 연령별 대화 주제 선호도
→ 대화 가이드 품질 지속 개선 (서비스 선순환)
```

### 11-2. 유치원/어린이집 연계

```
B2B2C 확장:
- 교사용 대시보드: 반 전체 이용 현황
- 가정-어린이집 연계 리포트
  예: "오늘 어린이집에서 공룡 주제를 다뤘어요. 집에서도 이어서 이야기해보세요"
- 교육 커리큘럼과 연동된 카드 팩 제공
```

### 11-3. 발달 마일스톤 추적

```
소아과 학회 기준 언어/인지 발달 마일스톤 연계:
- 만 4세: 이야기 구조 이해, 4음절 이상 단어 사용
- 만 5세: 인과관계 질문 이해, 숫자 10까지 세기
- 만 6세: 글자 인식 시작, 추론 능력 발달
→ 아이의 학습 데이터를 발달 마일스톤과 매핑하여
   "발달 단계에 맞는" 콘텐츠와 대화 가이드 제공
```

### 11-4. 다국어/다문화 가정 지원

```
한국어-영어 이중언어 가정:
- 카드별 언어 설정 (아침: 한국어 / 저녁: 영어)
- 이중언어 대화 가이드
  예: "오늘 영어로 'Black hole'을 배웠어요.
      집에서 '블랙홀'과 함께 알려주세요."
- 이민 가정을 위한 모국어 + 현지어 동시 지원
```

### 11-5. 정서 발달 트래킹

```
현재: 인지적 학습(단어, 정답률) 중심
발전 방향: 정서적 반응도 함께 추적

감정 키워드 분석:
- 아이가 "신나요!", "무서워요", "이상해요" 같은 감정 표현을 얼마나 했는가
- 어떤 주제에서 더 활발하게 반응했는가
→ 정서 발달 리포트 + 공감 대화 가이드
  예: "오늘 공룡 이야기에서 '무서워요'를 3번 말했어요.
      '무서운 게 있을 때 어떻게 하면 좋을까?' 이야기해보세요."
```

### 11-6. 플랫폼 확장 (장기)

```
Reading Buddy → 가족 연결 플랫폼으로 확장

현재: 아이 학습 도구
→ 가족 구성원 모두의 연결 플랫폼
  - 조부모 카드: 할머니/할아버지가 직접 녹음한 목소리로 동화 들려주기
  - 가족 스토리 카드: 가족이 함께 만든 스토리를 스피커로 공유
  - 가족 퀴즈 카드: 저녁 식사 전 가족이 함께 푸는 퀴즈
```

---

## 부록 A: MVP 구현 가이드 (Phase 1)

### 신규 추가할 파일/디렉토리

```
app/
└── parent/                          ← 신규 라우트 그룹
    ├── layout.tsx                   ← 모바일 목업 셸 레이아웃
    │   (데스크톱: 스마트폰 프레임 + 앱 UI / 모바일: 전체화면)
    ├── home/page.tsx                ← 홈 화면
    ├── reports/page.tsx             ← 리포트 화면
    ├── talk-guide/
    │   ├── page.tsx                 ← 대화 가이드 목록
    │   └── [guideId]/page.tsx       ← 대화 가이드 상세
    └── settings/page.tsx            ← 설정 화면

app/api/parent/
    ├── sessions/route.ts            ← 세션 로그 저장/조회
    ├── reports/route.ts             ← 이용/학습 현황 집계
    ├── talk-guide/route.ts          ← 대화 가이드 AI 생성
    └── profiles/route.ts            ← 아이 프로필 CRUD

data/parent/
    ├── profiles.json                ← 아이 프로필 저장
    ├── sessions/                    ← 세션 로그 (날짜별)
    │   └── {YYYY-MM-DD}.json
    └── talk-guides/                 ← 생성된 대화 가이드
        └── {profileId}/

components/parent/
    ├── MobileShell.tsx              ← 스마트폰 목업 프레임 컴포넌트
    ├── BottomNav.tsx                ← 하단 탭 네비게이션
    ├── TalkGuideCard.tsx            ← 대화 가이드 카드
    ├── SpeakerStatusCard.tsx        ← 스피커 현재 상태 카드
    └── UsageChart.tsx               ← 이용 현황 차트
```

### MobileShell 렌더링 방식

```tsx
// app/parent/layout.tsx 개념
// 데스크톱: 스마트폰 목업 프레임 안에 children 렌더링
// 모바일 기기: 그냥 전체 화면으로 children 렌더링

export default function ParentLayout({ children }) {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      {/* 데스크톱에서만 목업 프레임 표시 */}
      <div className="
        w-full max-w-[390px]          /* 스마트폰 너비 */
        md:shadow-2xl md:rounded-[40px] md:border-8 md:border-gray-800
        md:overflow-hidden md:min-h-[844px]  /* iPhone 14 Pro 기준 */
        relative bg-white
      ">
        {children}
      </div>
    </div>
  )
}
```

---

## 부록 B: 메인 PRD 연계 포인트

| 현재 구현 (PRD.md) | 부모님 앱 MVP 연계 방식 |
|---|---|
| `data/cards.json` | `/api/parent` 라우트에서 동일 파일 읽기/쓰기로 카드 제어 |
| `BookData.pages[].vocabulary` | 세션 종료 시 vocabulary 추출 → `data/parent/sessions/` 저장 |
| `app/api/story/finalize` | 스토리 완성 후 `data/parent/talk-guides/` 에 가이드 자동 생성 |
| `app/api/chat/route.ts` | 응답 후처리 훅 추가: 세션 로그 JSON 기록 |
| `data/prompt-versions/` | 설정 화면에서 난이도 파라미터 → 프롬프트 버전 활성화 API 호출 |
| `hooks/useSoundManager.ts` | 볼륨 설정값을 `data/parent/profiles.json`에 저장 후 스피커 반영 |
| `app/api/realtime/route.ts` | 세션 시작/종료 이벤트를 세션 로그에 기록 |
